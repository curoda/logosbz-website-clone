# logosbz-website-clone

Faithful static clone of https://logosbz.com/ (Lógos Bravo Zulu).

- `capture/` — raw per-page capture specs (screenshots, rendered HTML, computed styles, assets, metadata).
- `clone/` — the rebuilt static site deployed to Vercel.
